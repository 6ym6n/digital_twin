# Google Gemini API Configuration
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_api_key_here

# Optional: MQTT mode (external simulator e.g., MATLAB/Simulink)
# The backend uses the built-in simulator by default.
# To enable MQTT telemetry ingestion, set SENSOR_SOURCE=mqtt in your shell before starting the backend.
MQTT_HOST=localhost
MQTT_PORT=1883
MQTT_PUMP_ID=pump01
MQTT_BASE_TOPIC=digital_twin
